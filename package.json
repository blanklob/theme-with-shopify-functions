{
  "name": "functions-app",
  "private": true,
  "scripts": {
    "build": "shopify app build",
    "dev": "shopify app dev",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app env pull"
  },
  "dependencies": {
    "@shopify/app": "3.44.1",
    "@shopify/cli": "3.44.1"
  },
  "devDependencies": {
    "@shopify/shopify_function": "^0.0.3",
    "javy": "^0.0.3"
  }
}
